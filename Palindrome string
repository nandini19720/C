class Solution {
    int isPalindrome(String S) {
        if(S.length()==1){
            return 1;
        }
         for(int i=0;i<((S.length())/2);i++){
            if(S.charAt(i)!=(S.charAt(S.length()-i-1))){
                return 0;
            }
        }
        return 1;
       
    }
};
