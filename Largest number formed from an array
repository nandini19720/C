class Solution {
    String printLargest(String[] arr) {
        String s="";
        Arrays.sort(arr,(a,b)->(b+a).compareTo(a+b));
        for(int i=0;i<arr.length;i++){
            s+=arr[i];
        }
        return s;
            
            
        }
        
    }
