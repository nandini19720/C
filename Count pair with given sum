class Solution {
    int getPairsCount(int[] arr, int n, int k) {
        int count=0;
        Arrays.sort(arr);
        for(int i=0;i<n-1;i++){
            if(arr[i]+arr[i+1]==k){
                count++;
            }
            
        }
        return count;
        
    }
}
