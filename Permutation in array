class Solution {
    public boolean isPossible(long a[], long b[], long n, long k) {
        Arrays.sort(b);
        for(int i=0;i<n;i++){
            if(a[i]+b[i]>=k){
                return true;
            }
        }
        return false;
        
    }
}
