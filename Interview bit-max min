public class Solution {
    public int solve(int[] A) {
        Arrays.sort(A);
        int sum=0;
        for(int i=0;i<A.length;i++){
         sum=A[0]+A[A.length-1];
        }
        return sum;
    }
}

