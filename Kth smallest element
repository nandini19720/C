class Solution{
    public static int kthSmallest(int[] arr, int l, int r, int k) 
    { 
        Arrays.sort(arr);
        for(int i=0;i<arr.length;i++){
            return arr[k-1];
            
        }
        return -1;
       
    } 
}
